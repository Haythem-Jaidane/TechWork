{% block offre %}

<div class="content">
        <div class="row">
          <div class="col-12">
            <div class="card">
              <div class="card-header ">
                    <h2 class="card-title">Les Projets</h2>
              </div>
              <div class="card-body ">
                <div class="table-responsive">
                 <a href="{{ path( 'app_stats') }}"> STATISTIQUE</a>
                  <table class="table tablesorter" id ="">
                    
                   <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Domaine</th>
                
            </tr>
        </thead>
        <tbody>
        {% for projet in projets %}
            <tr>
                <td>{{ projet.id }}</td>
                <td>{{ projet.nom }}</td>
                <td>{{ projet.description }}</td>
                <td>{{ projet.domaine }}</td>
                
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
                  </table>
                   
                   <a href="{{ path( 'app_fichmedia_index2') }}"> consulter fichiers media</a>
                   <a href="{{ path( 'app_typeprojet_index2') }}"> consulter les categories</a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

{% endblock %}