{% block offre %}

<div class="content">
        <div class="row">
          <div class="col-12">
            <div class="card">
              <div class="card-header ">
                    <h2 class="card-title">Les Projets</h2>
              </div>
              <div class="card-body ">
                <div class="table-responsive">
                  <table class="table tablesorter" id ="">
                    
                   <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Domaine</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for projet in projets %}
            <tr>
                <td>{{ projet.id }}</td>
                <td>{{ projet.nom }}</td>
                <td>{{ projet.description }}</td>
                <td>{{ projet.domaine }}</td>
                <td>
                    <a href="{{ path('app_projet_show', {'id': projet.id}) }}">show</a>
                    <a href="{{ path('app_projet_edit', {'id': projet.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
                  </table>
                   <a href="{{ path('app_projet_new') }}">Create new</a>
                   <a href="{{ path( 'app_fichmedia_index') }}">Consulter fichiers media</a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

{% endblock %}